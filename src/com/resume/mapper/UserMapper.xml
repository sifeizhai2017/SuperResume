<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <!-- namespace:该mapper.xml映射文件的 唯一标识 -->
    <mapper namespace="com.resume.mapper.UserMapper">

    <resultMap id="userMap" type="com.resume.pojo.User">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Thu Feb 06 13:29:56 CST 2020.
        -->
        <id column="id" jdbcType="BIGINT" property="id"/>
        <id column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="email_addr" jdbcType="VARCHAR" property="emailAddr"/>
        <result column="email_password" jdbcType="VARCHAR" property="emailPassword"/>
        <result column="imap_server" jdbcType="VARCHAR" property="imapServer"/>
        <result column="smtp_server" jdbcType="VARCHAR" property="smtpServer"/>
        <result column="pop3_server" jdbcType="VARCHAR" property="pop3Server"/>
        <result column="gmt_create" jdbcType="TIMESTAMP" property="gmtCreate"/>
        <result column="gmt_modified" jdbcType="TIMESTAMP" property="gmtModified"/>
    </resultMap>

    <!-- 表必备三字段：id, gmt_create, gmt_modified -->
    <insert id="insertUser" parameterType="com.resume.pojo.User">
        INSERT INTO user(username, password, gmt_create, gmt_modified)
        VALUES (#{username}, #{password}, now(), now())
    </insert>

    <select id="getUserByUsername" parameterType="string" resultMap="userMap">
        SELECT *
        FROM user
        WHERE username = #{username}
    </select>

    <!-- 更新邮件账号和密码 -->
    <update id="updateEmailInfo" parameterType="com.resume.pojo.User">
        UPDATE user
        SET email_addr=#{emailAddr},
            email_password=#{emailPassword},
            gmt_modified=now()
        WHERE username = #{username}
    </update>

    <!-- 更新用户密码 -->
    <update id="updateUserPassword" parameterType="com.resume.pojo.User">
        UPDATE user
        SET password=#{password},
            gmt_modified=now()
        WHERE username = #{username}
    </update>

    <!-- 更新邮箱信息 -->
    <update id="updateServer" parameterType="com.resume.pojo.User">
        UPDATE user
        SET pop3_server=#{pop3Server},
            smtp_server=#{smtpServer},
            imap_server=#{imapServer}
        WHERE username = #{username}
    </update>

</mapper>
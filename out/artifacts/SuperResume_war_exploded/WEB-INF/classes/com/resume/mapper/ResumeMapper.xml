<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace:该mapper.xml映射文件的 唯一标识 -->
<mapper namespace="com.resume.mapper.ResumeMapper">

    <!-- 增加一条记录 -->
    <insert id="insertResume" parameterType="com.resume.pojo.Resume">
        INSERT INTO resume(username, company, position, send_time, is_read, gmt_create, gmt_modified)
        VALUES (#{username}, #{company}, #{position}, now(), #{read}, now(), now())
    </insert>

    <!-- 更新一条记录 -->
    <update id="updateTime" parameterType="com.resume.pojo.Resume">
        UPDATE resume
        SET is_read=#{read},
            gmt_modified=now(),
            receive_time=#{receiveTime},
            interview_time=#{interviewTime}
        WHERE username = #{username}
    </update>


</mapper>